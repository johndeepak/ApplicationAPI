@model IEnumerable<WebMVC.Models.PrcticeJoinModels>
<script src="http://localhost:14470/Scripts/jquery-1.10.2.min.js"></script>
<p>
    @Html.ActionLink("Create New", "AddOREdit")
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderNumber)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.Customername)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
      
        <td>
            @Html.DisplayFor(modelItem => item.OrderNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customername)
        </td>
      
        <td>
            @Html.ActionLink("Edit", "AddOREdit", new { id=item.orderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.orderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.orderId })
        </td>
    </tr>
}

</table>
@section scripts{

    <script>
        $(function () {

            var sucessmessage = '@TempData["SucessMessage"]'
            if (sucessmessage!="")
            alert(sucessmessage);
        });

    </script>
    
    }
